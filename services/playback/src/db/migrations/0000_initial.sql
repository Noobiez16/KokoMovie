-- Playback service uses DynamoDB — no PostgreSQL schema needed.
-- Tables are created programmatically via ensureTables() in dynamo.ts.
-- This file is a placeholder to maintain migration script consistency.
SELECT 1;
