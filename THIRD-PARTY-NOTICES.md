# Third-Party Notices

Copyright remains with the respective owners.

## FFmpeg

The packaged `ffmpeg-static` 5.3.0 dependency declares `GPL-3.0-or-later`. The bundled Linux x64 FFmpeg 7.0.2-static binary reports `--enable-gpl`, `--enable-version3`, and `--enable-static`. See https://ffmpeg.org/legal.html.

Before release, generate platform-specific notices from exact artifacts, preserve license texts, and provide corresponding source or another compliant source mechanism.

## The Movie Database

This product uses the TMDB API but is not endorsed or certified by TMDB.

TMDB terms and branding requirements apply: https://www.themoviedb.org/terms-of-use

## npm dependencies

`package-lock.json` defines the dependency graph and declared licenses. Release automation must generate and review an exact inventory; this summary does not replace license texts.
